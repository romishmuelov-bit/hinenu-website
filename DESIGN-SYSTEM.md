# מערכת העיצוב של הננו

מסמך אחד שמחזיק את שלושת הדפים ביחד. מי שנוגע באחד מהם — קורא את זה קודם.

---

## הרעיון המארגן

שלושת הדפים הם אותה יד, בשלוש שעות ביום.

| | הדף | האווירה | שעה |
|---|---|---|---|
| **התיישבות** | `mitzpe-gvolot/` | אדמה, היסטוריה, כובד | בין ערביים |
| **יזמות** | `index.html` | קולנועי, חשמלי, מהיר | לילה |
| **הרצאות** | `harzaot.html` | אוויר, עומק, פתיחות | בוקר |

מה שמשותף: אותן אותיות, אותה תנועה, אותה גיאומטריה, אותו גרעין.
מה ששונה: האור והצבע. זה כל ההבדל, וזה מספיק.

---

## 1. טיפוגרפיה — הוחלף

**לפני:** `index.html` טען 17 משפחות מגוגל בבקשה אחת + 8 `@font-face` מקומיים,
עם 21 הצהרות `font-family` שונות. אין לזה שם אחר מלבד כאוס.

**אחרי — שני פונטים בלבד:**

```css
--display : 'Reisinger Yonatan', 'Rubik', system-ui, sans-serif;   /* מקומי, fonts/ */
--sans    : 'Rubik', system-ui, sans-serif;                        /* Google */
```

Reisinger Yonatan הגיע מדף ההתיישבות והוא עכשיו פונט התצוגה של כל האתר.
יש לו משקל אחד בלבד (400) — זו לא מגבלה, זו ההחלטה. אין bold מזויף, אין
היררכיה שנבנית ממשקלים. **ההיררכיה נבנית מגודל ומאוויר.**

Rubik נושא הכל: גוף, תוויות, כפתורים, מספרים. 300 לגוף, 400 לתוויות,
500–700 לדגשים, 900 רק למספרים גדולים.

### מה נשאר בכוונה
- **Dana Yad** — כתב היד של המניפסט. זה אפקט, לא פונט.
- **OH Iftah Yavetz / OH Aner Shapiro / OH Netta Epstein** — פונטי *אותיות חיים*,
  קרויים על שם נופלים. הם בחתימה ובמכתב. **לא להחליף אותם בלי החלטה מפורשת.**
- **ui-monospace** — מצב מכונה בלבד.

### סקאלה
| תפקיד | גודל | פונט |
|---|---|---|
| Hero | `clamp(38px, 7vw, 92px)` | display 400 |
| כותרת מקטע | `clamp(27px, 3.7vw, 46px)` | display 400 |
| כותרת כרטיס | `clamp(20px, 2.2vw, 28px)` | display 400 |
| גוף | `clamp(14px, 1.5vw, 18px)` | sans 300 |
| תווית | `12–14px`, `letter-spacing:.18em` | sans 700 |

---

## 2. חמישה קווים שמשותפים לשלושת הדפים

### א. גרעין (grain)
`index.html` כבר מחזיק `.grain` — שכבת רעש SVG קבועה ב-`mix-blend-mode:overlay`.
זה החתימה הוויזואלית הכי זולה והכי מזוהה שיש. להעתיק לשלושת הדפים:

```css
.grain{position:fixed;inset:0;z-index:400;pointer-events:none;
  mix-blend-mode:overlay;
  background-image:url("data:image/svg+xml,...feTurbulence...")}
```
עוצמה: `.05` על רקע כהה, `.03` על רקע בהיר.

### ב. קו התקדמות
2px בראש העמוד, בצבע ההדגשה של הדף. כבר קיים ב-`mitzpe-gvolot` (`#boot`)
וב-`harzaot.html` (`.progress`). להוסיף ל-`index.html`.
זה מה שמאותת לגולש "אתה באותו אתר" עוד לפני שהוא קרא מילה.

### ג. תנועה
עקומה אחת, קבוע החלקה אחד:
```css
--ease: cubic-bezier(.16, 1, .3, 1);   /* כל מעבר, בכל דף */
```
```js
const EASE = 0.075;   // lerp על ערך הגלילה
current += (target - current) * EASE;
```
זה מה שגורם לשלושת הדפים להרגיש שנבנו על ידי אותו אדם, גם כשהם בצבעים הפוכים.

### ד. גיאומטריה
רדיוסים: `6 / 16 / 28 / 999`. שום ערך אחר.
רוחב תוכן: `1080px`. רוחב פנימי: `24px` דסקטופ, `6vw` נייד.

### ה. פוקוס
```css
:focus-visible{ outline:3px solid var(--accent); outline-offset:3px }
```
אותה צורה בכל דף, צבע לפי הדף. נגישות היא חלק מהמערכת, לא תוספת.

---

## 3. הפלטות — כאן הדפים מתפצלים

```css
/* התיישבות — אדמה */
--navy:#133458; --olive:#838921; --amber:#D99B21;
--pale:#FAF7BB; --red:#C1272D; --wine:#8E2230;
--bg:var(--navy); --ink:var(--pale); --accent:var(--amber);

/* יזמות — לילה. לא לגעת, זו הזהות. */
--bg:#070b16; --ink:#F2F0E9; --blue:#4257E6; --coral:#D96A50;
--accent:var(--blue);

/* הרצאות — בוקר */
--navy:#28346d; --peri:#829ce8; --sky:#cddcfb;
--sky-light:#eef3fd; --sand:#f7e7b3;
--bg:var(--sky-light); --ink:var(--navy); --accent:var(--peri);
```

**הכלל:** לכל דף יש רקע אחד, דיו אחד, וצבע הדגשה אחד. שאר הצבעים
בפלטה הם לתמונות ולמבטאים, לא לטקסט.

---

## 4. איך שומרים על ההפרדה בין יזמות להתיישבות

הן הכי קרובות זו לזו (שתיהן כהות), אז ההבדל חייב להיות מכוון:

| | יזמות | התיישבות |
|---|---|---|
| טמפרטורה | קר — כחול חשמלי על שחור | חם — ענבר וזית על נייבי |
| קצב | חיתוכים מהירים, מסך מלא | Ken Burns איטי |
| טיפוגרפיה | display צפוף, `letter-spacing` שלילי | display מרווח, שורות קצרות |
| תמונות | סצנות מבוימות, ניגודיות גבוהה | תיעוד, גוון אחיד וחמים |
| קול | "הגיע הזמן להתאהב בבעיה" — קריאה | "פעם ידענו לעשות את זה" — הצהרה |

אם מישהו מבלבל בין השניים — הבעיה כמעט תמיד בטמפרטורת הצבע, לא בפונט.

---

## 5. מה עוד שווה לעשות

1. **קובץ `shared.css` אחד** עם הטוקנים, ה-`@font-face`, ה-grain, הפוקוס והעקומה.
   שלושת הדפים מייבאים אותו ודורסים רק את הפלטה. כרגע כל דף מחזיק עותק משלו,
   וזה יתפצל שוב תוך חודש.
2. **מעבר בין דפים** — כל דף מסתיים בקישור לשני האחרים, בפורמט זהה.
   עכשיו הם שלושה אתרים שבמקרה חולקים דומיין.
3. **ציטוט לקוח אחד** בדף ההרצאות. זה החוסר השיווקי הכי גדול שנשאר.
